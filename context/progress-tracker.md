# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Auth (complete); `/editor` home shell stubbed → project dialogs next

## Current Goal

- `context/feature-specs/03-auth.md` is implemented and the post-sign-in 404 on `/editor` is unblocked. Next: `context/feature-specs/04-project-dialogs.md`.

## Completed

- Design system & UI primitives (`context/feature-specs/01-design-system.md`): shadcn/ui initialized (`components.json`, style `base-nova`, baseColor `neutral`, RSC + CSS variables); Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea added to `components/ui/`; `lucide-react` installed; `lib/utils.ts` created with `cn()` (clsx + tailwind-merge). `app/globals.css` rewritten so the Ghost AI dark palette from `context/ui-context.md` is the only theme (no `.dark` class, no light `:root`) and is wired both into shadcn's semantic tokens (`background`, `card`, `primary`, `border`, etc.) and into the custom utility names ui-context.md calls out (`bg-base`, `bg-surface`, `text-copy-primary`, `text-copy-muted`, `border-surface-border`, `text-brand`, `bg-accent-dim`, ...). Verified with `tsc --noEmit`, `next build`, and a dev-server render showing `--bg-base: #080809` compiled into the served CSS.
- Editor chrome (`context/feature-specs/02-editor-chrome.md`): `components/editor/editor-navbar.tsx` — `h-14` top bar, `border-b border-surface-border bg-base`, three equal flex sections (left holds the sidebar toggle using `PanelLeftClose` when open / `PanelLeftOpen` when closed; center and right are empty placeholders for later chapters). `components/editor/project-sidebar.tsx` — floating overlay (`fixed top-16 bottom-3 left-3 z-40 w-72`, `rounded-2xl`, `bg-surface/90` + backdrop blur) that slides in from the left via `translate-x` and never occupies layout space, with a `Projects` header + close button, shadcn `Tabs` (`My Projects` / `Shared`) whose panels each show a muted `h-8 w-8` icon empty state, and a full-width `New Project` button with a `Plus` icon pinned at the bottom. `components/editor/editor-dialog.tsx` — reusable dialog shell over `components/ui/dialog` taking `open`/`onOpenChange`/`title`/`description`/`footer`/`children`, styled with the Ghost AI tokens (`rounded-3xl`, `bg-elevated`, `border-surface-border`, `text-copy-primary`/`text-copy-muted`); no concrete dialogs built yet. Verified with `tsc --noEmit`, `next build`, and a temporary smoke route rendered against `next dev` (navbar, sidebar tabs, empty state, and `New Project` button all rendered; `bg-surface/90`, `text-copy-faint`, `border-surface-border` confirmed present in the compiled CSS) — the smoke route was removed afterwards.
- Auth (`context/feature-specs/03-auth.md`): `@clerk/ui` installed. `lib/clerk-appearance.ts` exports `clerkAppearance` — Clerk's `dark` theme from `@clerk/ui/themes` as the base, with every appearance variable (`colorBackground`, `colorForeground`, `colorPrimary`, `colorInput`, `colorBorder`, `colorDanger`/`colorSuccess`/`colorWarning`, `fontFamily`, ...) pointed at the Ghost AI CSS custom properties from `app/globals.css` (`var(--bg-surface)`, `var(--accent-primary)`, ...) — no hardcoded colors. `app/layout.tsx` wraps `<html>` in `ClerkProvider appearance={clerkAppearance}`. `components/auth/auth-shell.tsx` is the shared two-panel frame: on `lg` and up the viewport splits 50/50, with the left half a `bg-surface` panel under a flat `bg-accent-dim opacity-25` wash (no gradient) carrying the `Ghost` logo lockup, the headline, a tagline, a three-item feature list (icon + title + one-line description), and a copyright line; below `lg` the panel is hidden and only the centered Clerk form remains. `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` render `<SignIn />` / `<SignUp />` inside it. `proxy.ts` at the repo root (Next 16's rename of `middleware.ts`) runs `clerkMiddleware`, treats only `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` (and their sub-paths) as public, and calls `auth.protect()` on everything else. `app/page.tsx` is now a server-side router: `/` redirects to `/editor` when signed in and `/sign-in` when not. `UserButton` added to `EditorNavbar`'s right section, with Clerk's default menu and profile flows left untouched. Verified with `tsc --noEmit`, `eslint`, `next build` (build output lists `ƒ Proxy (Middleware)`), and live requests against `next dev`: `/`, `/editor`, and an arbitrary path all 307 to `/sign-in?redirect_url=...`; `/sign-in`, `/sign-up`, and `/sign-in/factor-one` return 200; the served sign-in HTML contains the left-panel copy and `var(--bg-surface)` / `var(--accent-primary)` / `var(--text-primary)` in Clerk's inlined appearance.
- Typography fix (found while implementing `03-auth.md`): `app/globals.css` defined `--font-sans: var(--font-sans)` and `--font-heading: var(--font-sans)` — self-referential, so both resolved to nothing and the app silently rendered in the browser's default font, and the `font-heading` utility used by `components/ui/card.tsx` and `components/ui/dialog.tsx` emitted no rule at all. Both now point at the `--font-geist-*` variables that `next/font` sets on `<html>`, with system fallbacks, and `--font-mono` was added for Geist Mono per `context/ui-context.md`. Verified in the compiled CSS: `html { font-family: var(--font-geist-sans), ... }`, `--default-font-family` / `--default-mono-font-family` resolve to Geist / Geist Mono, and `.font-heading` now emits a rule.
- `/editor` home shell (minimal unblocker, not a full spec chapter): signing in redirected to `/editor`, which had no `page.tsx` — `app/editor/` existed but was an empty directory, so Next created no route and every sign-in dead-ended on a 404. Added `app/editor/page.tsx` (a server component, so `context/feature-specs/07-wire-editor-home.md` can fetch the owned/shared project lists there) rendering `components/editor/editor-home.tsx` (`"use client"`), which owns `isSidebarOpen` and mounts the existing `EditorNavbar` + `ProjectSidebar` plus the editor-home empty state from `04-project-dialogs.md` (heading `Create a project or open an existing one`, its description, and a `New Project` button with a `Plus` icon; no cards). Deliberately scoped to unblock sign-in only: no dialogs, no state hook, no sidebar item actions, no mock data, no API. `EditorHome` is the client boundary those additions attach to, so `04` extends this file rather than reworking it. Verified with `tsc --noEmit`, `eslint`, `next build` (route list now shows `○ /editor`), and a rendered check through a temporary public smoke route: the empty state centers correctly, the sidebar toggle opens the overlay at `x=12` without shifting the centered content, and the toggle's `aria-label` flips to `Close sidebar`. The smoke route and its temporary `proxy.ts` public-path entry were removed afterwards and `proxy.ts` was confirmed byte-for-byte identical to before (signed-out probes: `/` `/editor` 307, `/sign-in` 200).

## In Progress

- None.

## Next Up

- `context/feature-specs/04-project-dialogs.md`.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Editor chrome components live in `components/editor/` and are presentational shells: they take state (`isSidebarOpen`, `isOpen`, `open`) and callbacks from their parent and hold no state of their own, so later chapters can own project/dialog state without rewriting them.
- The project sidebar is an overlay (`fixed`, `z-40`), never a layout column — opening it must not resize or push the canvas.
- Editor dialogs are composed through `EditorDialog` rather than by using `components/ui/dialog` directly, so the modal styling (`rounded-3xl`, elevated surface, token colors) stays defined in one place.
- Theme is dark-only: `app/globals.css` defines the Ghost AI palette once in `:root` (no `.dark` class, no light variant) and maps it onto shadcn's semantic tokens plus the custom utility names in `context/ui-context.md`. New components must consume those tokens (`bg-base`, `text-copy-*`, `border-surface-border`, `text-brand`, etc.) — no raw Tailwind color classes or hardcoded hex values.
- `components/ui/*` are shadcn-generated and should not be hand-edited; extend/wrap them elsewhere if custom behavior is needed.
- Route protection lives in `proxy.ts` (Next 16 renamed `middleware.ts` → `proxy.ts`; only one such file is allowed per project). It is deny-by-default: anything not matching the sign-in/sign-up env vars goes through `auth.protect()`.
- Public routes are derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` rather than hardcoded strings, so the public surface and the pages Clerk redirects to cannot drift apart. `createRouteMatcher` is deliberately unused — it is deprecated in `@clerk/nextjs` v7.
- Clerk appearance is configured once on `ClerkProvider` via `lib/clerk-appearance.ts`. New Clerk components inherit it; do not pass per-component `appearance` overrides unless a component genuinely needs to differ.
- Clerk's own user menu, profile, and sign-out flows are used as shipped — `UserButton` is rendered with no element-level customization.

## Session Notes

- `EditorNavbar`'s right section now holds only `UserButton`, and the center section is still intentionally empty; `context/feature-specs/08-editor-workspace-shell.md` adds the project name, share button, and AI sidebar toggle alongside it.
- `ProjectSidebar` renders placeholder empty states only. Project lists, per-item rename/delete actions, and the mobile backdrop arrive in `context/feature-specs/04-project-dialogs.md`.
- `EditorDialog` is unused so far by design — `04-project-dialogs.md` is the first consumer (create / rename / delete).
- The `New Project` buttons (editor home centre and sidebar footer) are intentionally inert — `04-project-dialogs.md` wires both to the Create dialog. `ProjectSidebar` already accepts an `onNewProject` prop for that.
- The editor home currently defaults the sidebar closed and passes no project data; `04` adds mock data and `07` replaces it with server-fetched lists.
- The previously noted unused `Button` import in `app/page.tsx` is gone; the file is now a redirect-only server component.
- Clerk's card is a fixed 400px on wide viewports and self-constrains to `calc(100% - 2rem)` below that, so the auth pages need no mobile width handling of their own — measured at 375px/430px viewports: card 335px/390px, no horizontal scroll.
- Clerk lays two social providers out side by side on a wide card regardless of `options.socialButtonsVariant`; matching a stacked layout would take element-level overrides of Clerk internals, which `03-auth.md` rules out. Left at Clerk's default.
- `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` were added to `.env.local` (Clerk's standard variable names). `proxy.ts` reads them, so changing either value moves both the public route and Clerk's redirect target together.
